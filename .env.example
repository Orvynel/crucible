# Copy this file to .env.local and paste your own Nansen API key.
# Get one at https://app.nansen.ai/api
# This key is read only by the offline data scripts (scripts/build-dataset.ts).
# It is never shipped to the browser.
NANSEN_API_KEY=your_nansen_api_key_here
